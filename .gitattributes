# Enforce UTF-8 encoding for all text files
* text=auto eol=lf

# Mark all .md files as text
*.md text working-tree-encoding=UTF-8

# Ensure shell scripts have correct line endings
*.sh text eol=lf

# PowerShell scripts
*.ps1 text eol=crlf
